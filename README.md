## .vim

```
git clone git://github.com/rbt328/.vim.git
```

## tags

```
ctags -R -I __THROW -I __wur -I __asm+ -I __nonnull+ -I __attribute__+ -I __attribute_pure__ \
--fields=+aiS --extra=+q --c-kinds=+p --c++-kinds=+p \
/usr/local/include /usr/include
```
